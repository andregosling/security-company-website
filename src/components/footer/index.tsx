import React, { useEffect, useState } from 'react';
import styles from './main.module.scss';

export const Footer = (props: any) => {
    return (
        <footer>
            <div className={styles.personFooter}> </div>
        </footer>
    )
}

